import numpy as np
arr_2d=np.array([[1,2,3],
                 [4,5,6.5]])
print(arr_2d.shape) # shape the number of rows and columns (or dimensions) in the array
print(arr_2d.ndim) # ndim stands for Number of Dimensions. It tells us how many dimensions (axes) a NumPy array has
print(arr_2d.size)
print(arr_2d.dtype) 